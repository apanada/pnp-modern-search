export interface ISearchVerticalsContainerState {
    
    /**
     * The current selected vertical key
     */
    selectedKey: string;

    hideFeedbackDialog: boolean;

    selectedFeedbackEmoji: string;

    selectedFeedbackOption: string;
}